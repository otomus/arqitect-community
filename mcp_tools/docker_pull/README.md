# docker_pull

Pull a Docker image from a container registry.

## Parameters

| Name  | Type   | Required | Description                              |
|-------|--------|----------|------------------------------------------|
| image | string | yes      | Image name to pull (e.g. 'nginx:latest') |

## Usage

```python
output = run("nginx:latest")
```
