{
  "name": "http_put",
  "version": "1.0.0",
  "description": "Send an HTTP PUT request to a URL",
  "implementations": {
    "python": "tool.py"
  },
  "author": {
    "github": "sentient-community"
  },
  "category": "web",
  "tags": [
    "http",
    "put",
    "request",
    "api",
    "update"
  ],
  "parameters": [
    {
      "name": "url",
      "type": "string",
      "description": "URL to send the request to",
      "required": true
    },
    {
      "name": "body",
      "type": "string",
      "description": "Request body (JSON string)",
      "required": true
    },
    {
      "name": "headers",
      "type": "string",
      "description": "JSON string of headers",
      "required": false,
      "default": "{}"
    }
  ],
  "requires_api_key": false
}