# window_list

List all open windows on the desktop. Uses platform-specific approaches: AppleScript on macOS, wmctrl on Linux, and pyautogui on Windows.

## Parameters

None.

## Usage

```python
result = run()
# Returns: "Found 5 window(s):\n- Finder\n- Terminal\n- ..."
```
