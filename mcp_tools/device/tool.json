{
  "name": "device",
  "version": "1.0.0",
  "description": "Get device status or list all connected IoT devices",
  "params": {
    "device_id": {
      "type": "string",
      "description": "Unique identifier of the device (required for status)",
      "required": false
    },
    "network": {
      "type": "string",
      "description": "Filter devices by network (optional, list only)",
      "required": false
    },
    "operation": {
      "type": "string",
      "description": "Operation to perform: status or list",
      "required": true
    }
  },
  "runtime": "python",
  "entry": "tool.py",
  "timeout": 30,
  "author": {
    "github": "sentient-community"
  },
  "category": "iot",
  "implementations": {
    "python": "tool.py"
  },
  "tags": [
    "iot",
    "device",
    "status",
    "list"
  ],
  "requires_api_key": false,
  "dependencies": {
    "python": [
      "requests"
    ]
  }
}
