# math_eval

Safely evaluate mathematical expressions.

## Usage

```python
result = run("sqrt(144) + 2 ** 3")
# Returns: "20"
```

Supports: +, -, *, /, //, %, **, sqrt, ceil, floor, log, sin, cos, tan, pi, e, abs, round, min, max.