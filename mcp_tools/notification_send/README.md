# notification_send

Send a push notification.

## Usage

```python
result = run("Alert", "Something happened")            # local (macOS)
result = run("Alert", "Something happened", "slack")  # named target
```