{
  "name": "db_schema",
  "version": "1.0.0",
  "description": "Return column names and types for a specific table, or for all tables if no table is specified.",
  "params": {
    "connection_id": {
      "type": "string",
      "description": "Connection token returned by db_connect"
    },
    "table": {
      "type": "string",
      "description": "Table name to inspect (optional — omit to get schema for all tables)"
    }
  },
  "runtime": "python",
  "entry": "tool.py",
  "timeout": 30
}
