# clipboard_write

Write text to the system clipboard. Uses pyperclip for cross-platform clipboard access.

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| text | string | yes | Text to write to the clipboard |

## Usage

```python
result = run("hello world")
# Returns: "Copied 11 character(s) to clipboard"
```
