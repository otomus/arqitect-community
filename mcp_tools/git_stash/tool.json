{
  "name": "git_stash",
  "version": "1.0.0",
  "description": "Stash or pop changes in a Git working directory",
  "params": {
    "message": {
      "type": "string",
      "description": "Optional message describing the stash (stash only)",
      "required": false
    },
    "ref": {
      "type": "string",
      "description": "Stash index to pop, e.g. '0' (optional, pop only, default 0)",
      "required": false
    },
    "operation": {
      "type": "string",
      "description": "Operation to perform: stash or pop",
      "required": true
    }
  },
  "runtime": "python",
  "entry": "tool.py",
  "timeout": 30,
  "author": {
    "github": "sentient-community"
  },
  "category": "git",
  "implementations": {
    "python": "tool.py"
  },
  "tags": [
    "git",
    "version-control",
    "stash",
    "pop"
  ],
  "requires_api_key": false,
  "dependencies": {
    "python": [
      "gitpython"
    ]
  }
}
