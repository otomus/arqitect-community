# camera

Photo and video capture.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y camera-mcp-server
```

## Authentication

No authentication required.

## Tools

- take_photo
- record_video
- list_cameras

## Capabilities

`camera` `photo` `video` `capture` `webcam`
